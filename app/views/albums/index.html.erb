<div class="album-indx">
  <% @albums.each do |album| %>
    <ul>
      <li>
        <%= Band.find(album.band_id).name %><br>
        <%= link_to album.title, album_path(album) %>
      </li> <br>
    </ul>
  <% end %>
</div>
